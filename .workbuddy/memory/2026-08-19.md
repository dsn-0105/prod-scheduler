# 2026-08-19 工作日志

## 生产排单系统（prod-scheduler）部署相关

- 仓库 `dsn-0105/prod-scheduler` 已 Public 部署到 GitHub Pages：`https://dsn-0105.github.io/prod-scheduler/`
- 自动备份两类触发源已默认关闭（`index.html` 中 `autoBackupOn=false`，且 `autoBackupNow` 首行改为 `if(!autoBackupOn)return` 修复关页面事件 `force=true` 绕过开关的漏洞）。手动「备份」按钮仍可用。
- **版本号机制（新增约定）**：`index.html:3008` 有 `build X.Y` 显示文本；部署工作流 `.github/workflows/deploy.yml` 的 build 步骤 `Inject build version` 用 sed 在每次 push 部署时自动替换为 `日期.全局run_number`（如 `2026-08-19.5`）。**后续每次更新只需 push，版本号会自动变更，无需手动改源码**（源码里的 `build 2026-08-19.1` 仅为初始占位，线上以工作流注入为准）。
- 用户本地推送用 PortableGit 完整路径：`& "C:\Users\WINDOWS\.workbuddy\binaries\PortableGit\versions\1.2.0\cmd\git.exe" -C "C:\Users\WINDOWS\WorkBuddy\2026-08-18-16-59-26" push`（机器未把 git 加入 PATH；凭据已缓存）。
- **订单管理界面改为明细行视图**：`renderOrders()` 中把每个订单的 `items` 展开成独立行；表格列包括「预计交货日期、单据日期、单据编号、客户、审核状态、商品编码、商品名称、型号、规格、单位、数量、已交付、未交付、进度%、商品行备注」；型号/规格/单位从产品主数据按 `productId` 自动 lookup 带出；操作按钮编辑/删除仍针对整个订单。
- **订单级列合并单元格**：同一订单的「预计交货日期、单据日期、单据编号、客户、审核状态、操作」使用 `rowspan` 合并；当用户按商品级列（如商品编码/数量等）排序时，自动取消合并以避免同一订单行被打散。

## 批量操作改造（行选择 + 上方工具栏）
- 需求：操作列移到字段上方；每行最前加选择框；勾选后点上方按钮操作；新增复制按钮；编辑/复制/生成排期仅支持单条（多选 alert 拒绝）；导出改为导出选中。
- 新增核心函数块（在 backup 段之前）：`selRows`/`lastRowIds` 状态、`toggleSel`/`selAll`、`bulkBar`(工具栏)、`bulkSingle`/`bulkDelete`/`bulkExport`、`suggestCopyId`、各 `copyXxx`、`orderFlatRowsOf`。
- `FORM_ACTIONS` 配置各模块(m→formKey/get/edit/del/copy/gen) 映射：product/order/inventory/dp_stockup/dp_pkg/delivery。
- 受影响表单均改造：产品、订单、库存、日排产(备货dp_stockup+包装dp_pkg)、交付计划。每行加 `<td class="sel-col">` 复选框 `onclick="toggleSel('m',idx,checked)"`；去除原行内「操作」列；表头加空选择列；工具栏放全选/编辑/复制/(生成排期)/删除/导出选中/模板/导入。订单选择框在合并首行 rowspan（按商品列排序时每行独立）。
- 各 modal 增加 `prefill` 第2参数（`productModal/orderModal/inventoryModal/planModal/deliveryModal`），供复制时打开「新增」并预填。
- 复制主键去重：`suggestCopyId` 对 id 类（product/order/delivery）自动加 `_2/_3` 后缀；库存/排产复制时日期置 `todayStr()/planDate` 避免复合主键冲突。
- `exportForm(formKey, rows)` 支持选中行（isSel 时文件名加「_选中N条」）；未选则全量。
- `switchTab` 切换时清空 `selRows`。
- 生产日报(renderDailyReport)为只读派生统计，未加选择/工具栏（无可增删改）。
- 本次改动已 `git commit`（0edd16f），但**尚未 push**（用户机器当时 github.com:443 网络不通，待网络恢复后本地 push）。

## 订单管理拆分「框架订单 / 库存订单」两类（可收起）
- 需求：订单管理拆为两类，同页可收起/展开。
- 数据模型新增 `orderType` 字段（`fields.order` 加 `orderType`，select，选项 `ORDER_TYPES=['框架订单','库存订单']`，head 级，必填，默认「框架订单」）；种子订单与 `migrateOrders` 缺省补 `框架订单`。`orderModal` 默认 `orderType:'框架订单'`，新增入口 `addOrderOfType(type)` 按类型预填。
- 渲染改造：`renderOrders()` 不再渲染单表，改为两段可折叠区块 `orderSection(k,formKey,title,sub,prodMap)`；两类分别用独立 formKey `order_frame`/`order_inv`，各自 `renderOrderTable(formKey,typeOrders,prodMap)`（保留合并单元格 + 批量/筛选独立状态）。
- 收起状态 `orderCollapse={frame:false,inv:false}`，`toggleOrderSection(k)` 翻转换 `renderContent()`；区块头部含标题/数量徽标/描述/「+新增」按钮（新增按钮 `event.stopPropagation()` 防误触收起）。
- `FORM_ACTIONS` 增加 `order_frame`/`order_inv`（formKey 均指向 `order`）；`bulkDelete`、`bulkExport` 条件分支覆盖这两个 formKey。
- 版本号 bump 至 `build 2026-08-19.2`；语法 `node --check` 通过；已 commit `2194b58`（未 push）。
